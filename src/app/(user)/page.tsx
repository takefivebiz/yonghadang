import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "VEIL — 베일에 가려진 진짜 나",
}

// TODO: 메인 페이지 구현 (PRD 5.2 메인 페이지 참고)
const HomePage = () => {
  return <div />
}

export default HomePage
